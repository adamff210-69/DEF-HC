# Optional ML extra for ContentRiskAnalyzer(demo_mode=False).
# Loads BAAI/bge-small-en-v1.5 via sentence-transformers and the logistic
# weights produced by scripts/train_classifier.py (sklearn trainer, spec
# Phase 3).  `datasets` is only needed by scripts/prepare_benchmarks.py;
# `matplotlib` only by scripts/make_figures.py.
-r requirements.txt
sentence-transformers>=3.0
torch>=2.3
scikit-learn>=1.4
datasets>=2.18
matplotlib>=3.8
